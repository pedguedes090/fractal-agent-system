[project]
name = "hethongagent-engine"
version = "0.1.0"
description = "LangGraph + OpenHands coding-agent engine for He Thong Agent."
requires-python = ">=3.12"
dependencies = [
  "langgraph>=1.2.5",
  "openhands-sdk>=1.28.1",
  "openhands-tools>=1.28.1",
]

[tool.uv]
package = false
